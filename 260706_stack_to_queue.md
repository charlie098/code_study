# 💡 스택 2개로 큐 구현하기 (Implement Queue using Stacks)

## 📌 개요
스택(Stack) 2개를 활용하여 선입선출(FIFO) 구조를 가지는 큐(Queue)를 구현한 프로젝트입니다.  
기존의 정석적인 대기 방식에서 벗어나, **양쪽 스택의 인덱스를 제어하며 데이터를 동적으로 이동시키는 독창적인 '핑퐁(Ping-Pong) 알고리즘'**을 설계하여 문제를 해결했습니다.

---

## 🛠️ 핵심 알고리즘 및 메커니즘

### 🔄 데이터 핑퐁 이동 (Ping-Pong Logic)
데이터를 무조건 한쪽에 모아두는 방식 대신, `pop()`이나 `peek()`이 호출될 때 **현재 출력해야 하는 데이터(가장 오래된 데이터) 하나만 남겨두고 나머지 데이터를 반대쪽 스택으로 슬라이싱 복사한 뒤 원본 스택을 비우는(`clear()`) 방식**을 사용합니다.

1. **상황 1 (`stack1`에 데이터가 있고 `stack2`가 빌 때)**
   - `stack1[1:]`에 해당하는 최신 데이터들을 `stack2`로 토스합니다.
   - `stack1[0]`에 위치한 가장 오래된 데이터를 변수에 저장한 뒤 `stack1.clear()`를 수행하고 반환합니다.
2. **상황 2 (`stack1`이 비고 `stack2`에 데이터가 있을 때)**
   - 반대로 `stack2[1:]` 데이터를 `stack1`로 토스합니다.
   - `stack2[0]` 데이터를 변수에 저장한 뒤 `stack2.clear()`를 수행하고 반환합니다.

---

## ✨ 기능별 조건 분기 및 예외 처리

- **`push(x)`**: 데이터를 `이미 차있는 stack`에 `둘다 빈 경우 stack1에` 차곡차곡 쌓습니다. 단 `stack1, 2 모두`에 데이터가 있으면 **`-1**을 반환합니다 
- **`pop()` / `peek()`**:
  - `if (stack1 존재, stack2 빔)` ➡️ 데이터를 `stack2`로 밀어내고 `[0]`번 인덱스 제어
  - `elif (stack1 빔, stack2 존재)` ➡️ 데이터를 `stack1`로 밀어내고 `[0]`번 인덱스 제어
  - `else` (두 스택에 데이터가 동시에 존재하거나 둘 다 빌 때) ➡️ 예외 상황으로 인지하여 **`-1`**을 안전하게 반환
- **`empty()`**: 두 스택이 모두 원소 개수가 0일 때만 `True`를 반환하고, 데이터가 하나라도 남아있다면 `False`를 반환하여 큐의 공백 상태를 명확하게 판단합니다.

---

## 🧠 배운 점 및 회고
- **파이썬 슬라이싱과 `.pop()`의 관계**: `for atom in stack1[1:]`을 순회하면서 내부에서 `.pop()`을 호출하면 리스트 뒷부분부터 원소가 삭제되어 인덱스 꼬임 현상이 발생할 수 있음을 컴퓨터 시뮬레이션을 통해 깊이 이해했습니다.
- **인덱스 탐색의 중요성**: 큐(FIFO)의 특성을 살리기 위해 가장 먼저 들어온 데이터의 위치인 `[0]`번 방을 정확히 콕 집어 구출해 내는 흐름을 정립했습니다.
- **예외 처리의 꼼꼼함**: 두 스택의 상태가 모호할 때 `else` 블록을 활용해 예외 값을 리턴하도록 유도하여 시스템의 논리적 안정성을 높였습니다.
