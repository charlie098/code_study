# 📊 [TIL] 최적화된 최소값 스택(MinStack) 알고리즘 구조 분석

> **날짜**: 2026-07-07  
> **주제**: 메모리 절약형 투 스택(Two-Stack) 연동 메커니즘  
> **핵심 키워드**: `#Stack` `#Algorithm_Optimization` `#Time_Complexity` `#Space_Efficiency`

---

## 💡 1. 핵심 과제 및 제약 조건
- **목표**: 데이터의 추가/삭제와 동시에, 현재 스택 내의 **최소값(Minimum)**을 즉시 찾아내는 구조 설계
- **출제 조건**: 모든 연산(`push`, `pop`, `top`, `getMin`)은 반드시 **$O(1)$ (상수 시간)** 내에 완료되어야 함
  - *즉, 데이터가 100만 개 쌓여있어도 최소값을 찾기 위해 루프를 돌거나 검색하는 과정이 없어야 함.*

---

## ⚖️ 2. 데이터 탐색 방식 전후 비교

| 평가 항목 | ❌ 기존 방식 (단일 스택 탐색) | 🎯 개선 방식 (투 스택 타이밍 매칭) |
| :--- | :--- | :--- |
| **시스템 구조** | 하나의 스택만 사용 | 메인 데이터 스택 + 최소값 기록용 장부 스택 |
| **최소값 연산** | `getMin()` 호출 시마다 전체 데이터 스캔 | 데이터가 들어오고 나갈 때 계산기를 미리 두드림 |
| **시간 복잡도** | **$O(N)$** (데이터 양이 많아질수록 정비례해서 느려짐) | **$O(1)$** (데이터가 아무리 많아도 즉시 반환) |
| **공간 효율성** | 메모리 추가 소모 없음 | **조건부 기록**으로 장부 스택의 메모리 낭비 극대화 방지 |
| **종합 평가** | 대규모 시스템이나 실시간 처리에서 탈락 | **코딩 테스트 및 고성능 시스템 적합 기준 달성** |

---

## 🔄 3. 핵심 알고리즘 메커니즘 (타이밍 매칭 시스템)

모든 데이터를 복사해서 장부 스택의 높이를 강제로 맞추는 대신, **'진짜 최소값의 자격이 있을 때만'** 장부에 기록하고 빼는 타이밍 제어 방식입니다.



### ① 데이터 추가 시 (`push`)
1. 메인 데이터 스택에는 무조건 새로운 값을 쌓습니다.
2. 장부 스택과 비교를 시작합니다.
   - **장부 스택이 완전히 비어있거나**,
   - **새로 들어온 값이 현재 장부의 최상단 값(현재 최소값)보다 `작거나 같으면(<=)`**
   - ➡️ 장부 스택에도 새 값을 추가하여 최소값의 계보를 갱신합니다.
   - *이외의 상황(더 큰 값이 들어온 경우)에는 장부에 기록하지 않고 패스하여 메모리를 아낍니다.*

### ② 데이터 삭제 시 (`pop`)
1. 메인 데이터 스택의 맨 위(가장 최신 값)를 제거하면서 값을 확인합니다.
2. 방금 메인 스택에서 제거된 값이 **현재 장부 스택의 맨 위(최소값)와 `완전히 일치(==)`하는지** 대조합니다.
   - **두 값이 같다면**: 현재 최소값의 유효 수명이 다한 것이므로, 장부 스택에서도 맨 위 값을 함께 제거(`pop`)합니다.
   - **두 값이 다르다면**: 메인에서 빠진 값이 최소값에 영향을 주지 않는 큰 값이라는 뜻이므로, 장부는 그대로 유지합니다.

### ③ 최소값 확인 시 (`getMin`)
- 장부 스택을 탐색할 필요 없이, 언제나 장부 스택의 **맨 꼭대기 값(`[-1]`)**만 슥 읽어서 즉시 반환합니다.

---

## 🧠 4. 오늘의 핵심 깨달음 (Key Takeaway)
1. **파이썬 편리함의 함정**: `min()` 같은 내장 함수는 코드 작성이 직관적이지만 내부적으로는 전체 배열을 훑는 노가다(O(N))를 하기 때문에 성능 최적화의 걸림돌이 된다.
2. **트레이드 오프(Trade-off)**: 장부 스택이라는 미세한 메모리 공간을 추가로 사용하는 대신, 무한대의 데이터 증가에도 버티는 '초고속 연산 성능'을 트레이드했다.
3. **정교한 조건 설계의 중요성**: 데이터 입력 시의 `<=`(작거나 같다) 조건과 출력 시의 `==`(같다) 조건을 소름 돋게 일치시킴으로써, 중복된 최소값이 들어와도 데이터의 정렬 톱니바퀴가 절대 어긋나지 않는 견고한 구조를 완성했다.
